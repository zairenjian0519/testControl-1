# 工业数据采集与OPC UA服务器系统
## 架构设计与概要设计文档

## 1. 系统概述

本系统是一个基于Modbus TCP协议的工业数据采集和OPC UA服务器系统，实现了从Modbus设备读取数据并转换为OPC UA标准格式提供给客户端访问的功能。系统支持设备自动发现、动态OPC UA节点管理、数据实时更新等核心功能。

### 1.1 主要功能
- 设备自动发现（基于ADDP协议）
- Modbus TCP数据采集
- OPC UA数据模型构建与维护
- 动态节点管理（基于设备状态）
- 配置文件管理
- IPv6网络支持

### 1.2 系统特点
- 配置化设计：通过CSV文件定义数据模型
- 动态节点管理：根据设备状态自动添加/删除OPC UA节点
- 高可靠性：支持Modbus连接重连机制
- 标准化：遵循OPC UA标准协议
- IPv6支持：适应现代工业网络需求

## 2. 架构设计

### 2.1 整体架构

系统采用分层架构设计，从上到下分为：

```
┌─────────────────────────┐
│     OPC UA 服务层       │
├─────────────────────────┤
│     数据处理层          │
├─────────────────────────┤
│     网络通信层          │
└─────────────────────────┘
```

#### 2.1.1 网络通信层
- **ADDP协议**：实现设备自动发现功能
- **Modbus TCP协议**：实现与工业设备的数据通信
- **OPC UA协议**：提供标准化的工业数据访问接口

#### 2.1.2 数据处理层
- **CSV解析**：解析数据模型配置文件
- **数据转换**：PLC数据类型到OPC UA数据类型的转换
- **配置管理**：处理JSON配置文件

#### 2.1.3 OPC UA服务层
- **节点管理**：动态添加/删除OPC UA节点
- **数据更新**：更新OPC UA变量值
- **客户端访问**：处理OPC UA客户端请求

### 2.2 组件架构

```
┌────────────────────────────────────────────────────────────────────┐
│                        主程序 (main_loop.cpp)                     │
├────────────────────────────────────────────────────────────────────┤
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────────┐ │
│  │  ADDP协议模块  │  │  OPC UA服务器  │  │    Modbus轮询线程      │ │
│  │   (addp_protocol.cpp) │  (opcua_server.c) │   (modbusPollingThread) │ │
│  └───────────────┘  └───────────────┘  └─────────────────────────┘ │
│  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────────┐ │
│  │   CSV解析器    │  │ Modbus客户端   │  │  配置管理模块          │ │
│  │  (csv_parser.c) │  (modbus_client.c) │   (cJSON)              │ │
│  └───────────────┘  └───────────────┘  └─────────────────────────┘ │
└────────────────────────────────────────────────────────────────────┘
```

### 2.3 数据流程

```
1. 系统启动，读取config.json配置文件
2. 解析uploadtable.csv数据模型文件
3. 初始化OPC UA服务器
4. 创建Modbus客户端，建立TCP连接
5. 启动Modbus轮询线程，定期读取数据
6. 根据设备状态变量动态管理OPC UA节点
7. 将Modbus数据转换为OPC UA格式并更新
8. 处理OPC UA客户端请求
```

## 3. 核心模块设计

### 3.1 ADDP协议模块

**功能**：实现设备自动发现功能，允许客户端扫描网络中的设备。

**实现方式**：
- 基于UDP multicast实现
- 使用IPv6多播地址ff03::c和端口6060
- 支持设备扫描请求和响应消息

#### 3.1.1 协议常量

```cpp
namespace ADDP {
    constexpr uint16_t PROTOCOL_ID = 0xABDE;     // 协议ID
    constexpr uint8_t VERSION = 0x01;             // 协议版本
    constexpr uint8_t SCAN_REQUEST = 0x01;        // 扫描请求消息类型
    constexpr uint8_t DEVICE_RESPONSE = 0x02;     // 设备响应消息类型
    constexpr const char* MULTICAST_ADDRESS = "ff03::c";  // 多播地址
    constexpr uint16_t UDP_PORT = 6060;           // UDP端口
    constexpr uint32_t TIMEOUT_MS = 1000;         // 超时时间
    constexpr size_t HEADER_SIZE = 32;            // 消息头大小
    constexpr size_t BUS_INFO_SIZE = 34;          // 总线信息大小
}
```

#### 3.1.2 数据结构

**DeviceInfo类**：设备信息结构
```cpp
class DeviceInfo {
public:
    DeviceInfo();
    ~DeviceInfo();
    
    void init(const char* deviceName, const char* model, const char* ipv6Address, const char* macAddress);
    void addBus(uint8_t busId, const char* description, uint8_t nodeCount);
    void setOpcuaInfo(uint16_t port, const char* path);
    void printInfo() const;
    
    uint8_t deviceType;       // 设备类型
    uint8_t ipv6Address[16];  // IPv6地址
    uint8_t macAddress[6];    // MAC地址
    char deviceName[32];      // 设备名称
    char model[32];           // 设备型号
    uint8_t busCount;         // 总线数量
    uint16_t opcuaPort;       // OPC UA端口
    char opcuaPath[32];       // OPC UA路径
    BusInfo* buses;           // 总线信息数组
};
```

**BusInfo结构体**：总线信息结构
```cpp
struct BusInfo {
    uint8_t busId;            // 总线ID
    uint8_t enabled;          // 总线启用状态
    uint8_t nodeCount;        // 节点数量
    char description[31];     // 总线描述
};
```

#### 3.1.3 接口函数

**解析扫描请求消息**
```cpp
bool parseScanRequest(const uint8_t* buffer, int size, uint32_t& sequenceNumber, uint8_t* clientMac, uint8_t* clientIpv6);
```
- **功能**：解析客户端发送的ADDP扫描请求消息
- **参数**：
  - `buffer`：接收到的消息缓冲区
  - `size`：消息大小
  - `sequenceNumber`：输出参数，序列号
  - `clientMac`：输出参数，客户端MAC地址
  - `clientIpv6`：输出参数，客户端IPv6地址
- **返回值**：解析成功返回true，失败返回false

**构建设备响应消息**
```cpp
int buildDeviceResponse(const DeviceInfo& device, uint32_t sequenceNumber, const uint8_t* clientMac, const uint8_t* clientIpv6, uint8_t* buffer);
```
- **功能**：构建ADDP设备响应消息
- **参数**：
  - `device`：设备信息
  - `sequenceNumber`：序列号（与请求一致）
  - `clientMac`：客户端MAC地址（与请求一致）
  - `clientIpv6`：客户端IPv6地址（与请求一致）
  - `buffer`：输出参数，构建的响应消息
- **返回值**：响应消息大小

**网络函数**
```cpp
ErrorCode networkCreateMulticastSocket(Network* network);
ErrorCode networkReceiveData(Network* network, uint8_t* buffer, int* size, struct sockaddr_in6* senderAddr);
ErrorCode networkSendData(Network* network, const uint8_t* buffer, int size, const struct sockaddr_in6* destAddr);
```
- **功能**：创建多播套接字、接收数据、发送数据

#### 3.1.4 消息格式

**扫描请求消息格式**（32字节）：
- 协议ID：2字节（0xABDE）
- 版本：1字节（0x01）
- 消息类型：1字节（0x01，表示扫描请求）
- 消息长度：2字节
- 序列号：4字节
- 客户端MAC地址：6字节
- 客户端IPv6地址：16字节

**设备响应消息格式**：
- 消息头：32字节（与请求消息头类似）
- 设备信息：90字节
  - 设备类型：1字节
  - IPv6地址：16字节
  - MAC地址：6字节
  - 设备名称：32字节
  - 设备型号：32字节
  - 总线数量：1字节
  - OPC UA端口：2字节
  - OPC UA路径：32字节
- 总线信息：每个总线34字节，共busCount个
  - 总线ID：1字节
  - 启用状态：1字节
  - 节点数量：1字节
  - 描述：31字节

#### 3.1.5 工作流程

1. 服务端创建UDP多播套接字，绑定到端口6060
2. 客户端发送扫描请求消息到多播地址ff03::c:6060
3. 服务端接收扫描请求，解析客户端信息
4. 服务端构建设备响应消息，包含设备信息和总线信息
5. 服务端将响应消息发送给客户端（使用单播）
6. 客户端接收响应，获取设备信息

### 3.2 CSV解析器

**功能**：解析uploadtable.csv文件，定义设备变量和Modbus地址映射关系。

**实现方式**：
- 逐行读取CSV文件
- 解析modbusAddr、name、registerType、plcDatatype、desc等字段
- 处理特殊情况（如desc为undefined时替换为status）
- 构建CSVRecord结构体数组

**关键结构**：
```c
typedef struct {
    int modbusAddr;
    char name[100];
    RegisterType registerType;
    PLCDatatype plcDatatype;
    char desc[100];
    char deviceName[50];
    char nodeName[100];
} CSVRecord;
```

**关键函数**：
- `parseCSV()`：解析CSV文件
- `getPLCDatatype()`：转换PLC数据类型
- `getRegisterType()`：转换寄存器类型

### 3.3 Modbus客户端

**功能**：实现与Modbus设备的通信，读取寄存器数据。

**实现方式**：
- 使用libmodbus库实现Modbus TCP通信
- 支持读取线圈、离散输入、输入寄存器、保持寄存器
- 实现连接重连机制

**关键结构**：
```c
typedef struct {
    modbus_t *ctx;
} ModbusClient;
```

**关键函数**：
- `modbus_client_init()`：初始化Modbus客户端
- `modbus_client_connect()`：建立连接
- `modbus_client_read_registers()`：读取寄存器
- `modbus_client_read_input_registers()`：读取输入寄存器

### 3.4 OPC UA服务器

**功能**：实现OPC UA服务器功能，提供标准化的数据访问接口。

**实现方式**：
- 使用open62541库实现OPC UA服务器
- 动态创建设备节点和变量节点
- 支持基于设备状态的节点管理

**关键结构**：
```c
// OPC UA变量结构体
typedef struct {
    UA_NodeId node_id;
    int modbus_addr;
    RegisterType register_type;
    PLCDatatype plc_datatype;
    char name[100];
    void *value;
} OPCUAVariable;

// 设备节点结构体
typedef struct {
    UA_NodeId node_id;
    char name[50];
    OPCUAVariable *variables;
    int variable_count;
} DeviceNode;
```

**关键函数**：
- `createObjectNode()`：创建对象节点
- `createVariableNode()`：创建变量节点
- `addDeviceNodes()`：添加设备节点和变量
- `deleteDeviceNodes()`：删除设备节点和变量
- `updateOPCUAVariables()`：更新OPC UA变量值

### 3.5 Modbus轮询线程

**功能**：定期从Modbus设备读取数据并更新OPC UA变量。

**实现方式**：
- 独立线程运行
- 按寄存器类型分组读取数据
- 实现连接状态检查和重连机制
- 动态管理设备节点

**关键流程**：
```
1. 初始化Modbus客户端
2. 进入轮询循环
3. 检查连接状态，必要时重连
4. 读取输入寄存器
5. 读取保持寄存器
6. 读取离散输入
7. 更新OPC UA变量
8. 动态管理设备节点
9. 等待下一个轮询周期
```

## 4. 数据模型设计

### 4.1 OPC UA节点结构

```
spssps (设备)
  ├─ DeviceInfo (属性)
  └─ Buses (对象)
     ├─ Bus 0 (对象)
        ├─ BusStatus
        └─ Nodes
           ├─ AutbusPADev1 (设备节点)
              ├─ AutbusPADev1_status (状态变量)
              ├─ AutbusPADev1_slot0_InData1 (变量)
              └─ ...
           ├─ AutbusPADev2 (设备节点)
              └─ ...
```

### 4.2 设备状态管理

**功能**：根据设备状态变量动态管理OPC UA节点。

**实现方式**：
- 每个设备有一个状态变量（如AutbusPADev1_status）
- 状态值为1表示设备在线，为0表示设备离线
- 在线时添加设备节点和变量，离线时删除

**关键函数**：
- `isDeviceStatusVariable()`：判断是否为状态变量
- `checkDeviceStatus()`：检查设备状态
- `addDeviceNodes()`：添加设备节点
- `deleteDeviceNodes()`：删除设备节点

## 5. IPv6地址动态分配与使用

### 5.1 IPv6地址池配置

**功能**：管理IPv6地址的动态分配，为设备提供唯一的IPv6地址。

**配置结构**（config.json）：
```json
"ipv6_address_pool": {
    "start_address": "2001:eaca:0101:0000:001E:CD00:0201:0000",
    "end_address": "2001:eaca:0101:0000:001E:CD00:0201:1005",
    "prefix_length": 64,
    "max_lease_count": 1000
}
```

**字段解释**：

| 字段 | 类型 | 说明 |
|------|------|------|
| start_address | 字符串 | 地址池起始IPv6地址 |
| end_address | 字符串 | 地址池结束IPv6地址 |
| prefix_length | 整数 | 前缀长度 |
| max_lease_count | 整数 | 最大租约数量 |

### 5.2 地址动态分配机制

#### 5.2.1 实现方式

- **地址范围**：从配置的start_address到end_address之间的IPv6地址
- **分配策略**：基于设备发现请求，为每个请求的设备分配一个唯一的IPv6地址
- **地址管理**：
  - 使用链表或数组管理已分配和未分配的地址
  - 支持地址租约和回收机制
  - 确保地址的唯一性和有效性

#### 5.2.2 分配流程

```
1. 接收客户端的ADDP扫描请求
2. 检查客户端是否已分配地址
3. 如果未分配，从地址池选择一个可用地址
4. 将地址记录在设备信息中
5. 构建设备响应消息，包含分配的IPv6地址
6. 将响应发送给客户端
```

### 5.3 IPv6网络通信

#### 5.3.1 多播通信

- **多播地址**：ff03::c
- **UDP端口**：6060
- **实现方式**：
  ```cpp
  // 创建IPv6 UDP套接字
  network->multicastSocket = socket(AF_INET6, SOCK_DGRAM, IPPROTO_UDP);
  
  // 绑定到端口
  memset(&network->multicastAddr, 0, sizeof(network->multicastAddr));
  network->multicastAddr.sin6_family = AF_INET6;
  network->multicastAddr.sin6_port = htons(ADDP_UDP_PORT);
  network->multicastAddr.sin6_addr = in6addr_any;
  
  // 加入多播组
  struct sockaddr_in6 groupAddr;
  memset(&groupAddr, 0, sizeof(groupAddr));
  groupAddr.sin6_family = AF_INET6;
  InetPton(AF_INET6, ADDP_MULTICAST_ADDRESS, &groupAddr.sin6_addr);
  setsockopt(network->multicastSocket, IPPROTO_IPV6, IPV6_JOIN_GROUP, 
            (char*)&groupAddr, sizeof(groupAddr));
  ```

#### 5.3.2 设备发现流程

1. **客户端发送扫描请求**：
   - 使用IPv6多播地址ff03::c和UDP端口6060
   - 包含客户端MAC地址和IPv6地址

2. **服务端接收请求**：
   ```cpp
   // 接收ADDP扫描请求
   uint8_t buffer[1024];
   int size;
   struct sockaddr_in6 senderAddr;
   ErrorCode result = networkReceiveData(&network, buffer, &size, &senderAddr);
   
   // 解析扫描请求
   uint32_t sequenceNumber;
   uint8_t clientMac[6];
   uint8_t clientIpv6[16];
   if (addpParseScanRequest(buffer, size, &sequenceNumber, clientMac, clientIpv6)) {
       // 处理扫描请求
   }
   ```

3. **服务端构建设备响应**：
   ```cpp
   // 构建设备响应消息
   uint8_t response[1024];
   int responseSize = addpBuildDeviceResponse(
       &deviceInfo, sequenceNumber, clientMac, clientIpv6, response);
   ```

4. **服务端发送响应**：
   ```cpp
   // 发送设备响应给客户端
   ErrorCode sendResult = networkSendData(
       &network, response, responseSize, &senderAddr);
   ```

### 5.4 IPv6地址在OPC UA中的使用

- **OPC UA节点ID**：使用IPv6地址作为命名空间标识符
- **设备标识**：将分配的IPv6地址作为设备的唯一标识
- **通信地址**：客户端使用分配的IPv6地址连接到OPC UA服务器

### 5.5 错误处理与恢复

- **地址耗尽处理**：当地址池耗尽时，拒绝新设备的请求
- **地址冲突检测**：定期检查地址使用情况，避免冲突
- **地址回收机制**：回收长期不活动设备的地址

## 6. 配置管理

### 6.1 config.json配置文件

**功能**：配置系统的网络参数、IPv6地址池和Modbus TCP连接参数。

**完整结构**：
```json
{
    "description": "设备侧软件 - IPv6 组播 + 地址池配置",

    "ipv6_multicast": {
        "listen_port": 6060,
        "multicast_ip": "ff03::c",
        "bind_nic_name": "以太网",
        "nic_index": 12
    },

    "ipv6_address_pool": {
        "start_address": "2001:eaca:0101:0000:001E:CD00:0201:0000",
        "end_address": "2001:eaca:0101:0000:001E:CD00:0201:1005",
        "prefix_length": 64,
        "max_lease_count": 1000
    },

    "modbus_tcp": {
        "server_ip": "192.168.1.123",
        "server_port": 502,
        "polling_interval": 1000,
        "reconnect_interval": 5000
    }
}
```

**字段解释**：

| 字段 | 类型 | 说明 |
|------|------|------|
| description | 字符串 | 配置文件描述信息 |

#### 6.1.1 IPv6组播配置

| 字段 | 类型 | 说明 |
|------|------|------|
| listen_port | 整数 | 监听的UDP端口号 |
| multicast_ip | 字符串 | IPv6多播地址 |
| bind_nic_name | 字符串 | 绑定的网络接口名称 |
| nic_index | 整数 | 网络接口索引 |

#### 6.1.2 IPv6地址池配置

| 字段 | 类型 | 说明 |
|------|------|------|
| start_address | 字符串 | 地址池起始IPv6地址 |
| end_address | 字符串 | 地址池结束IPv6地址 |
| prefix_length | 整数 | 前缀长度 |
| max_lease_count | 整数 | 最大租约数量 |

#### 6.1.3 Modbus TCP配置

| 字段 | 类型 | 说明 |
|------|------|------|
| server_ip | 字符串 | Modbus服务器IP地址 |
| server_port | 整数 | Modbus服务器端口号 |
| polling_interval | 整数 | 轮询间隔（毫秒） |
| reconnect_interval | 整数 | 重连间隔（毫秒） |

**加载方式**：使用cJSON库解析JSON文件，将配置信息加载到相应的结构体中。

### 6.2 uploadtable.csv数据模型文件

**功能**：定义设备变量和Modbus地址映射关系，是系统数据模型的核心配置文件。

**完整示例**：
```csv
modbusAddr,name,register,datatype,desc
300001,AutbusPADev1_slot0_var1.AutbusPADev1_slot0_var1,Input Registers,USINT,InData1
300002,AutbusPADev1_slot0_var2.AutbusPADev1_slot0_var2,Input Registers,UINT,InData2
300003,AutbusPADev1_slot0_var3.AutbusPADev1_slot0_var3,Input Registers,ULINT,timer
300015,null.null,Input Registers,UINT,undefined
400001,AutbusPADev1_slot0_var10.AutbusPADev1_slot0_var10,Holding Registers,USINT,OutData1
```

**字段解释**：

| 字段 | 类型 | 说明 |
|------|------|------|
| modbusAddr | 整数 | Modbus寄存器地址，格式为：<br>- 1xxxx: 离散输入<br>- 2xxxx: 线圈<br>- 3xxxx: 输入寄存器<br>- 4xxxx: 保持寄存器 |
| name | 字符串 | 变量名称，格式为：<br>- 完整名称：设备名_槽位_变量名.设备名_槽位_变量名<br>- null.null：表示与上一行相同的设备和槽位 |
| register | 字符串 | 寄存器类型：<br>- Input Registers：输入寄存器<br>- Holding Registers：保持寄存器<br>- Discrete Inputs：离散输入<br>- Coils：线圈 |
| datatype | 字符串 | PLC数据类型：<br>- BOOL：布尔值<br>- USINT：无符号8位整数<br>- UINT：无符号16位整数<br>- ULINT：无符号32位整数<br>- INT：有符号16位整数<br>- DINT：有符号32位整数<br>- REAL：浮点数 |
| desc | 字符串 | 变量描述：<br>- 普通描述：如"温度值"、"压力值"<br>- undefined：表示设备状态变量 |

**解析规则**：

1. **设备名称提取**：
   - 从name字段的第一个_分隔部分提取设备名称（如"AutbusPADev1"）
   - 如果name为"null.null"，则继承上一行的设备名称

2. **变量命名规则**：
   - **状态变量**：当desc为"undefined"时，变量名为：设备名称 + "_status"（如"AutbusPADev1_status"）
   - **普通变量**：变量名为：name的前两个_分隔部分 + "_" + desc（如"AutbusPADev1_slot0_InData1"）

3. **特殊情况处理**：
   - **null.null处理**：如果当前行的name为"null.null"，则使用上一行的设备和槽位信息
   - **undefined处理**：如果desc为"undefined"，则将其替换为"status"，并将变量识别为设备状态变量

4. **数据类型映射**：
   | PLC数据类型 | OPC UA数据类型 |
   |------------|--------------|
   | BOOL | Boolean |
   | USINT | Byte |
   | UINT | UInt16 |
   | ULINT | UInt32/UInt64 |
   | INT | Int16 |
   | DINT | Int32 |
   | REAL | Float |

5. **访问权限映射**：
   | 寄存器类型 | OPC UA访问权限 |
   |----------|--------------|
   | Input Registers | ReadOnly |
   | Discrete Inputs | ReadOnly |
   | Holding Registers | ReadWrite |
   | Coils | ReadWrite |

**加载方式**：使用CSV解析器逐行读取文件，构建CSVRecord结构体数组，作为系统数据模型的基础。

## 6. 错误处理与重连机制

### 6.1 Modbus连接重连

**功能**：当Modbus连接断开时自动重连。

**实现方式**：
- 在轮询线程中检查连接状态
- 连接失败时设置connected标志为0
- 等待reconnect_interval毫秒后重新尝试连接

**关键代码**：
```c
if (!connected) {
    if (modbus_client_connect(&global_data->modbus_client) == MODBUS_SUCCESS) {
        fprintf(stdout, "Modbus slave connect success\n");
        connected = 1;
    } else {
        fprintf(stderr, "Failed to connect to Modbus server. Retrying in %d ms...\n", 
                global_data->modbus_config.reconnect_interval);
        Sleep(global_data->modbus_config.reconnect_interval);
        continue;
    }
}
```

### 6.2 错误日志

**功能**：记录系统运行过程中的错误信息。

**实现方式**：
- 使用fprintf输出错误信息到标准错误流
- 记录连接失败、读取失败等关键错误

## 7. 编译与构建

### 7.1 编译环境

- Windows平台
- MinGW-w64编译器
- 静态链接库

### 7.2 构建命令

```bash
mingw32-make
```

### 7.3 依赖库

- libopen62541.a：OPC UA库
- libmodbus.a：Modbus库
- ws2_32.lib：Windows网络库
- iphlpapi.lib：IP帮助库

## 8. 系统启动流程

```
1. 初始化Winsock
2. 读取config.json配置文件
3. 解析uploadtable.csv数据模型文件
4. 初始化OPC UA服务器
5. 创建Modbus客户端
6. 启动Modbus轮询线程
7. 运行OPC UA服务器主循环
8. 等待信号退出
9. 清理资源
```

## 9. 扩展与维护

### 9.1 新增设备支持

1. 在uploadtable.csv中添加新设备的变量定义
2. 确保设备状态变量正确配置
3. 重启系统或重新加载配置

### 9.2 修改轮询间隔

1. 修改config.json中的polling_interval值
2. 重启系统或重新加载配置

### 9.3 故障排查

- 检查Modbus服务器IP和端口配置
- 检查网络连接
- 查看系统输出的错误日志
- 检查uploadtable.csv文件格式

## 10. 总结

本系统实现了一个完整的工业数据采集和OPC UA服务器功能，具有以下特点：

- 配置化设计，便于扩展和维护
- 动态节点管理，提高系统灵活性
- 支持设备自动发现，简化部署
- 高可靠性，支持连接重连机制
- 标准化接口，易于与其他系统集成

系统采用分层架构和模块化设计，各组件职责清晰，便于扩展和维护。通过CSV文件定义数据模型，实现了配置化开发，大大降低了系统集成的复杂度。