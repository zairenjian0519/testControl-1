# 编译使用说明

## 1. 工程说明

本工程生成 Windows 控制器程序 `addp_controller.exe`，主要功能包括：

- 监听 AUTBUS/ADDP IPv6 组播发现请求。
- 启动 OPC UA Server，默认端口 `4840`。
- 从 `uploadtable.csv` 解析设备和属性变量。
- 通过 Modbus TCP 轮询数据。
- 为控制器、设备节点、变量节点分配 IPv6 地址，并配置到 Windows 网卡。

当前推荐使用 MinGW-w64 编译。工程内置静态库：

- `lib/libopen62541.a`
- `lib/libmodbus.a`

## 2. 目录结构

```text
testControl-1/
  build.ps1              PowerShell 编译脚本，推荐使用
  build.bat              MSVC 编译脚本，备用
  Makefile               MinGW make 编译入口
  config.json            运行配置
  uploadtable.csv        设备和变量映射表
  include/               第三方库头文件
  lib/                   第三方静态库
  src/                   源码
  build/                 编译输出目录
```

## 3. 编译环境

推荐环境：

- Windows 10/11 或 Windows Server。
- MinGW-w64，需提供 `g++`。
- PowerShell。

运行时要求：

- 程序需要管理员权限运行，否则添加/删除 Windows 网卡 IPv6 地址可能失败。
- `config.json` 中的 `ipv6_multicast.nic_index` 必须对应实际网卡接口索引。

查看网卡接口索引：

```powershell
netsh interface ipv6 show interfaces
```

## 4. 推荐编译方式

在工程根目录执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\build.ps1
```

编译成功后生成：

```text
build\addp_controller.exe
build\config.json
build\uploadtable.csv
```

`build.ps1` 会先删除旧的 `build` 目录，再重新编译所有源文件，并复制运行所需配置文件。

## 5. Makefile 编译方式

Debug 编译：

```powershell
mingw32-make debug
```

Release 编译：

```powershell
mingw32-make release
```

清理：

```powershell
mingw32-make clean
```

## 6. MSVC 编译方式

工程提供 `build.bat`，但当前第三方库是 `.a` 静态库，更适合 MinGW 链接。只有在确认 MSVC 可链接当前库文件，或已替换为 MSVC 兼容库后，才建议使用：

```cmd
build.bat
```

## 7. 运行方式

建议在管理员 PowerShell 或管理员 CMD 中运行：

```powershell
cd D:\Interest_Group\project1ai_testControl\testControl-1\build
.\addp_controller.exe
```

常用参数：

```text
-h, --help       显示帮助
-l, --log        启用日志输出
-f, --log-file   指定日志文件
-v, --verbose    启用 DEBUG 日志
```

示例：

```powershell
.\addp_controller.exe --log --verbose
```

## 8. 配置文件说明

程序启动时读取 `config.json`。

### device

```json
"device": {
    "name": "newpro3101",
    "model": "ATB-controler",
    "opcua_port": 4840,
    "opcua_path": "/autbus/controller"
}
```

- `name`：控制器名称。
- `model`：控制器型号。
- `opcua_port`：ADDP 响应中公布的 OPC UA 端口。
- `opcua_path`：ADDP 响应中公布的 OPC UA 路径。

### ipv6_multicast

```json
"ipv6_multicast": {
    "listen_port": 6060,
    "multicast_ip": "ff03::c",
    "bind_nic_name": "Ethernet",
    "nic_index": 12
}
```

- `listen_port`：ADDP/IPv6 UDP 监听端口。
- `multicast_ip`：IPv6 组播地址。
- `nic_index`：Windows 网卡接口索引，必须正确。

### ipv6_address_pool

```json
"ipv6_address_pool": {
    "start_address": "2001:eaca:0101:0000:001e:cd00:0201:0000",
    "end_address": "2001:eaca:0101:0000:001e:cd00:0201:1005",
    "prefix_length": 64,
    "max_lease_count": 1000,
    "skip_as_source": true,
    "batch_add_limit": 300,
    "batch_add_delay_ms": 4000
}
```

- `start_address` / `end_address`：程序管理的 IPv6 地址池范围。
- `prefix_length`：添加到网卡时使用的前缀长度。
- `skip_as_source`：添加 IPv6 地址时设置 `SkipAsSource=true`，避免这些变量地址参与普通出站源地址选择。
- `batch_add_limit`：每累计添加多少个新 IPv6 地址后触发一次延迟标记。
- `batch_add_delay_ms`：达到阈值后，在处理下一个在线设备前延迟的毫秒数。

### modbus_tcp

```json
"modbus_tcp": {
    "server_ip": "192.168.1.123",
    "server_port": 502,
    "polling_interval": 1000,
    "reconnect_interval": 5000
}
```

- `server_ip` / `server_port`：Modbus TCP 从站地址。
- `polling_interval`：轮询间隔，单位毫秒。
- `reconnect_interval`：断线重连间隔，单位毫秒。

### log

```json
"log": {
    "enable_log": false,
    "log_level": "INFO",
    "log_file": "application.log",
    "log_file_count": 2,
    "log_file_size": 1048576
}
```

## 9. IPv6 地址管理注意事项

程序会把设备和变量 IPv6 地址配置到 Windows 网卡。大量地址添加时，Windows 的 NLA、SSDP、IPsec Policy Agent 等服务可能短时间占用较高 CPU。

当前程序已实现以下优化：

- 使用 Windows IP Helper API `CreateUnicastIpAddressEntry`，不走 `netsh`。
- 设置 `SkipAsSource=true`。
- 每批达到阈值后，在下一个在线设备处理前延迟，不打断当前设备变量创建。
- Ctrl+C 正常退出时清理本程序添加的地址。
- 退出时扫描地址池范围内残留地址并清理。

如果需要确认地址是否添加：

```powershell
netsh interface ipv6 show address
```

如果需要手工删除某个地址：

```powershell
netsh interface ipv6 delete address <接口索引> <IPv6地址>
```

## 10. OPC UA 连接示例

外部客户端可通过任一已配置到网卡的变量 IPv6 地址访问 OPC UA Server：

```text
opc.tcp://[2001:eaca:0101:0000:001e:cd00:0201:0001]:4840
```

实际地址以程序分配和网卡配置结果为准。

## 11. 常见问题

### 添加 IPv6 地址失败

检查：

- 是否以管理员权限运行。
- `nic_index` 是否正确。
- 地址池是否耗尽。
- 网卡 IPv6 是否启用。

### Ctrl+C 后地址未删除

检查日志中是否出现：

```text
IPv6 allocation cache cleanup finished
```

如果没有该日志，说明程序没有走到正常清理路径。任务管理器强杀、关闭控制台窗口等方式不能保证执行进程内清理。

### 启动时 CPU 高

建议调整：

- `batch_add_limit`: 100 / 200 / 300
- `batch_add_delay_ms`: 2000 / 4000
- 确认 `skip_as_source` 为 `true`

